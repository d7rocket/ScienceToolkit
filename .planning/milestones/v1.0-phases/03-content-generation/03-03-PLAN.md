---
phase: 03-content-generation
plan: 03
type: execute
wave: 1
depends_on:
  - 03-02
files_modified:
  - .claude/skills/science/SKILL.md
autonomous: false
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04
  - CONT-05
  - CONT-06
  - CITE-01
  - CITE-02
  - CITE-03
gap_closure: true

must_haves:
  truths:
    - "Caption is 400-600 words — Rule 3 self-check has a stop clause blocking Step 6 until word count confirmed >= 400"
    - "Topic keyword appears in caption first sentence — Rule 3 self-check explicitly mandates keyword verification"
  artifacts:
    - path: ".claude/skills/science/SKILL.md"
      provides: "Strengthened Rule 3 self-check with stop clause and keyword mandate"
      contains: "Do NOT proceed to Step 6"
  key_links:
    - from: ".claude/skills/science/SKILL.md Rule 3 self-check"
      to: ".claude/skills/science/SKILL.md Step 6"
      via: "Conditional stop clause — Step 6 blocked until word count >= 400 and keyword confirmed"
      pattern: "Do NOT proceed to Step 6"
    - from: ".claude/skills/science/SKILL.md Rule 3 Paragraph 1"
      to: "output caption first sentence"
      via: "Keyword verification instruction"
      pattern: "topic keyword appears in the first sentence"
---

<objective>
Close the last remaining CONT-04 verification gap: the caption self-check clause is present but advisory, producing 283-word captions instead of the 400-word minimum. The topic keyword is also missing from the first sentence.

Purpose: The current self-check says "count the words, if under 400, expand" but the model treats expansion as optional and stops early. A stronger stop clause that blocks progression to Step 6 until the word count is explicitly confirmed >= 400, plus an explicit keyword-in-first-sentence mandate, will close the gap.

Output: Updated `.claude/skills/science/SKILL.md` Rule 3 with strengthened self-check.
</objective>

<execution_context>
@/home/deveshd/.claude/get-shit-done/workflows/execute-plan.md
@/home/deveshd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-generation/03-CONTEXT.md
@.planning/phases/03-content-generation/03-VERIFICATION.md
@.planning/phases/03-content-generation/03-02-SUMMARY.md

@.claude/skills/science/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strengthen Rule 3 self-check with stop clause and keyword mandate</name>
  <files>.claude/skills/science/SKILL.md</files>
  <read_first>
    - .claude/skills/science/SKILL.md (current Rule 3 at lines 252-268 — the self-check clause at line 264 is the edit target)
    - .planning/phases/03-content-generation/03-VERIFICATION.md (root cause analysis: "self-check instruction is advisory in form")
    - output/2026-03-16-crispr-gene-editing.md (the 283-word caption that exposed the gap — first sentence is "What happens when you turn bacteria's greatest survival trick into their biggest vulnerability?" which lacks the topic keyword "CRISPR")
  </read_first>
  <action>
Make THREE targeted edits to SKILL.md Rule 3. Do NOT touch any other Rule or Step.

**Edit 1 — Replace the self-check paragraph at line 264**

Replace the entire paragraph that starts with `**Self-check — caption word count:**` (line 264) with this stronger version:

```
**Self-check — caption word count and keyword:** After drafting the caption, perform these checks before proceeding:

1. **Word count gate:** Count the words in the caption. If under 400, you are not done. Expand Paragraph 2 (Context) by adding 2-3 more sentences: historical context of the problem, what researchers tried previously, how the field evolved to this point. Then expand Paragraph 4 (Significance) by adding 2-3 more sentences: who specifically is affected, what changes in clinical/practical terms, what open questions remain. Recount. Repeat this expansion cycle until the count reaches at least 400 words. Do not stop at 200-300 words — 400 words is a hard floor, not a suggestion.

2. **Keyword gate:** Verify the topic keyword appears in the first sentence of Paragraph 1 (the Hook). If the keyword is absent, rewrite the first sentence to include it. Example: if the topic is "CRISPR gene editing", the first sentence must contain "CRISPR" — e.g., "What happens when CRISPR meets its toughest challenge yet?" A hook that avoids naming the topic fails this check.

**Do NOT proceed to Step 6 until both checks pass.** If you cannot confirm (a) word count >= 400 and (b) topic keyword in the first sentence, return to the caption and fix it. This is a blocking gate — Step 6 must not execute on a non-compliant caption.
```

**Edit 2 — Strengthen Paragraph 2 and 4 length guidance**

In the paragraph arc list (lines 256-260), update Paragraph 2 and Paragraph 4 descriptions:

Replace:
```
- **Paragraph 2 (Context):** Background needed to appreciate the focal finding. 2–3 sentences.
```
With:
```
- **Paragraph 2 (Context):** Background needed to appreciate the focal finding. Aim for 4-6 sentences: cover the history of the problem, prior approaches that fell short, and why this finding matters now. This paragraph carries the most expansion weight for hitting the 400-word floor.
```

Replace:
```
- **Paragraph 4 (Significance):** Why it matters, what it changes, what prior assumption it challenges.
```
With:
```
- **Paragraph 4 (Significance):** Why it matters, what it changes, what prior assumption it challenges. Aim for 3-5 sentences: be specific about who is affected, what practical outcomes shift, and what questions remain open.
```

**Edit 3 — No other changes**

Do NOT modify Rules 1, 2, 4, 5, 6 or Steps 1-4, 6-7. Only Rule 3 is touched.
  </action>
  <verify>
    <automated>grep -q "Do NOT proceed to Step 6" /home/deveshd/Documents/ScienceToolkit/.claude/skills/science/SKILL.md && grep -q "Word count gate" /home/deveshd/Documents/ScienceToolkit/.claude/skills/science/SKILL.md && grep -q "Keyword gate" /home/deveshd/Documents/ScienceToolkit/.claude/skills/science/SKILL.md && grep -q "blocking gate" /home/deveshd/Documents/ScienceToolkit/.claude/skills/science/SKILL.md && grep -q "4-6 sentences" /home/deveshd/Documents/ScienceToolkit/.claude/skills/science/SKILL.md && grep -q "3-5 sentences" /home/deveshd/Documents/ScienceToolkit/.claude/skills/science/SKILL.md && echo "PASS: Stop clause, keyword gate, and paragraph guidance all present" || echo "FAIL: Missing required edits"</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md contains the exact string "Do NOT proceed to Step 6 until both checks pass."
    - SKILL.md contains "Word count gate:" with instruction to count and expand if under 400
    - SKILL.md contains "Keyword gate:" with instruction to verify topic keyword in first sentence
    - SKILL.md contains "blocking gate — Step 6 must not execute on a non-compliant caption"
    - SKILL.md Paragraph 2 guidance says "4-6 sentences" (not "2-3 sentences")
    - SKILL.md Paragraph 4 guidance says "3-5 sentences"
    - SKILL.md still contains "400 words is a hard floor"
    - SKILL.md Rules 1, 2, 4, 5, 6 are unchanged (grep "Self-check — cliff-hangers" and "Self-check — inline glosses" still present)
    - SKILL.md Steps 1-4 and 6-7 are unchanged
  </acceptance_criteria>
  <done>Rule 3 self-check replaced with stop-clause version that blocks Step 6 until word count >= 400 and keyword confirmed in first sentence. Paragraph 2 and 4 length guidance expanded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke-test caption word count and keyword after stop clause fix</name>
  <read_first>
    - .claude/skills/science/SKILL.md (confirm stop clause is in place)
    - .planning/phases/03-content-generation/03-VERIFICATION.md (gap to verify: CONT-04 caption 400+ words, keyword in first sentence)
  </read_first>
  <what-built>Strengthened Rule 3 self-check with a conditional stop clause that blocks Step 6 until (a) caption word count >= 400 and (b) topic keyword present in the first sentence. Also expanded paragraph length guidance for Paragraphs 2 and 4.</what-built>
  <how-to-verify>
    1. Run `/science CRISPR gene editing` in Claude Code
    2. Open the generated output file at `output/YYYY-MM-DD-crispr-gene-editing.md`
    3. Check the two CONT-04 conditions:
       - [ ] **Word count:** Run `sed -n '/^## Caption$/,/^---$/p' output/YYYY-MM-DD-crispr-gene-editing.md | grep -v '^## Caption$' | grep -v '^---$' | grep -v '^$' | wc -w` — expect 400 or more
       - [ ] **Keyword:** Read the first sentence of the caption — it must contain "CRISPR"
    4. Confirm no regressions on previously passing criteria:
       - [ ] 5-7 slides with cliff-hangers on body slides
       - [ ] Exactly 5 hashtags
       - [ ] Citations with URLs
       - [ ] Caption under 2,100 characters: `sed -n '/^## Caption$/,/^---$/p' output/YYYY-MM-DD-crispr-gene-editing.md | grep -v '^## Caption$' | grep -v '^---$' | wc -c` — expect under 2100
  </how-to-verify>
  <action>User runs the skill and verifies the caption meets the 400-word floor with topic keyword in the first sentence.</action>
  <verify>
    <automated>ls output/*crispr* 2>/dev/null && echo "PASS: Output file exists" || echo "FAIL: No output file generated"</automated>
  </verify>
  <acceptance_criteria>
    - Output file exists at output/YYYY-MM-DD-crispr-gene-editing.md
    - Caption word count >= 400 (verified by user via wc -w on caption section)
    - Topic keyword "CRISPR" appears in the caption's first sentence (verified by user reading)
    - Caption character count <= 2100 (verified by user via wc -c)
    - No regressions: 5-7 slides, cliff-hangers, 5 hashtags, citations with URLs
  </acceptance_criteria>
  <resume-signal>Type "approved" if caption is 400+ words with keyword in first sentence, or describe what still fails</resume-signal>
  <done>User has confirmed CONT-04 is fully closed: caption >= 400 words, topic keyword in first sentence, and no regressions on other requirements.</done>
</task>

</tasks>

<verification>
After Task 1, verify SKILL.md edits:
- Rule 3 contains stop clause "Do NOT proceed to Step 6 until both checks pass"
- Rule 3 contains "Word count gate" and "Keyword gate" as numbered checks
- Paragraph 2 guidance expanded to "4-6 sentences"
- Paragraph 4 guidance expanded to "3-5 sentences"
- Rules 1, 2, 4, 5, 6 unchanged
- Steps 1-4, 6-7 unchanged

After Task 2 (smoke test), verify output:
- CONT-04: Caption >= 400 words, topic keyword in first sentence
- No regressions on CONT-01 through CONT-06, CITE-01 through CITE-03
</verification>

<success_criteria>
- SKILL.md Rule 3 has a stop clause that explicitly blocks Step 6 until word count and keyword checks pass
- Running `/science CRISPR gene editing` produces a caption of 400+ words with "CRISPR" in the first sentence
- Caption stays under 2,100 characters
- All previously passing requirements (CONT-01, CONT-02, CONT-03, CONT-05, CONT-06, CITE-01, CITE-02, CITE-03) still pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-generation/03-03-SUMMARY.md`
</output>
