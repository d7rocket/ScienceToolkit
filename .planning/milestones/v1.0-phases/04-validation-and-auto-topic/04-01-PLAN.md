---
phase: 04-validation-and-auto-topic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/science/SKILL.md
autonomous: true
requirements:
  - TOPIC-01
  - TOPIC-02

must_haves:
  truths:
    - "Running /science with no argument triggers RSS discovery and presents a topic candidate with feed attribution"
    - "If user rejects the top candidate, alternatives #2 and #3 are offered"
    - "Running /science with an explicit topic that was covered in the last 14 days shows a diversity warning but proceeds"
    - "After a successful run, output/topic-log.json contains an entry with date, topic, field, and slug"
    - "A second run appends to topic-log.json (does not overwrite)"
    - "Auto-discovery skips candidates that match a topic-log.json entry within the 14-day window"
  artifacts:
    - path: ".claude/skills/science/SKILL.md"
      provides: "Step 1 rewritten with three-branch decision table (auto-discover, manual with dedup, rejection alternatives)"
      contains: "auto-discovery"
    - path: ".claude/skills/science/SKILL.md"
      provides: "Step 6 extended with topic-log.json append logic"
      contains: "topic-log.json"
  key_links:
    - from: "SKILL.md Step 1"
      to: "output/topic-log.json"
      via: "Read tool to check 14-day dedup before confirming topic"
      pattern: "output/topic-log.json"
    - from: "SKILL.md Step 6"
      to: "output/topic-log.json"
      via: "Read then Write to append new entry after output file is written"
      pattern: "topic-log.json"
    - from: "SKILL.md Step 1 auto-discovery"
      to: "RSS feed URLs from Step 4 Phase A"
      via: "Same 4 feed URLs used for topic extraction"
      pattern: "sciencedaily.com/rss|phys.org/rss|nature.com/nature.rss|arstechnica.com"
---

<objective>
Add auto-topic discovery and diversity tracking to the science skill.

Purpose: Make the skill hands-off for daily use by auto-discovering trending topics from RSS feeds when no argument is provided, and tracking covered topics in a JSON log to avoid repetition within 14 days. Covers requirements TOPIC-01 (auto-pick) and TOPIC-02 (diversity tracking).

Output: SKILL.md Step 1 rewritten with three-branch decision table, Step 6 extended with topic-log.json append, Step 7 extended with topic log line.
</objective>

<execution_context>
@/home/deveshd/.claude/get-shit-done/workflows/execute-plan.md
@/home/deveshd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-validation-and-auto-topic/04-CONTEXT.md
@.planning/phases/04-validation-and-auto-topic/04-RESEARCH.md
@.claude/skills/science/SKILL.md
@examples/output-sample.md

<interfaces>
<!-- Key structure the executor needs from SKILL.md -->

From .claude/skills/science/SKILL.md:
- Step 1 (lines 11-15): Current confirm-topic logic — to be REPLACED
- Step 4 Phase A (lines 33-152): RSS feed URLs for ScienceDaily, Phys.org, Nature News, Ars Technica — reuse same URLs in discovery
- Step 6 (lines 356-359): Current write-output logic — to be EXTENDED with topic-log.json append
- Step 7 (lines 362-370): Current terminal summary — to be EXTENDED with topic log line

RSS feed URLs already in Step 4 Phase A:
- https://www.sciencedaily.com/rss/all.xml
- https://phys.org/rss-feed/science-news/
- https://www.nature.com/nature.rss
- WebSearch "site:arstechnica.com/science {TOPIC} {CURRENT_YEAR}" (no RSS)

topic-log.json schema (new file, created on first run):
```json
[
  {
    "date": "2026-03-16",
    "topic": "CRISPR antibiotic resistance",
    "field": "Genetics / Synthetic Biology",
    "slug": "crispr-gene-editing"
  }
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite SKILL.md Step 1 with auto-discovery and diversity tracking</name>
  <files>.claude/skills/science/SKILL.md</files>
  <read_first>
    - .claude/skills/science/SKILL.md (current Step 1 to replace, Step 4 Phase A for RSS URLs)
    - .planning/phases/04-validation-and-auto-topic/04-CONTEXT.md (locked decisions on discovery method, diversity tracking, UX flow)
    - .planning/phases/04-validation-and-auto-topic/04-RESEARCH.md (Pattern 1-2 instruction text, Pattern 5 confirmation UX, Pitfall 1-4 avoidance)
  </read_first>
  <action>
Replace the entire `## Step 1: Confirm topic` section in SKILL.md with a new `## Step 1: Discover or confirm topic` section. The new Step 1 must contain three clearly labeled branches in a decision-table format:

**CASE A: `$ARGUMENTS` is empty (auto-discovery)**

Insert the following instruction text (adapting from RESEARCH.md Pattern 1, Pattern 2, and Pattern 5):

1. Fetch all 4 RSS feeds simultaneously using parallel WebFetch calls (no topic filter):
   - `WebFetch https://www.sciencedaily.com/rss/all.xml`
   - `WebFetch https://phys.org/rss-feed/science-news/`
   - `WebFetch https://www.nature.com/nature.rss`
   - `WebSearch "site:arstechnica.com/science science news [CURRENT_YEAR]"` (broad discovery query, no topic)

2. From each feed, extract the titles of the 5 most recent items (published within the last 48 hours where pubDate is available; otherwise take the top 5 items).

3. From the collected titles, identify 3-5 candidate topics by extracting the primary subject noun phrase from each title. Normalize to a short label (2-5 words). Example: "Scientists discover new CRISPR variant targeting antibiotic resistance" -> "CRISPR antibiotic resistance".

4. Count how many feeds mention each candidate (by semantic overlap, not exact match). Sort candidates descending by cross-feed count, then by recency.

5. "Strong candidate" threshold: 2+ feeds mention the same topic. If no topic appears in 2+ feeds, all candidates are weak — fall back to `WebSearch "trending science news today [CURRENT_YEAR]"` and extract the top 3 results as candidates ranked by recency.

6. **Dedup check** (applies to each candidate in ranked order):
   - Read `output/topic-log.json`. If the file does not exist, treat it as an empty array — do NOT fail.
   - For each candidate topic (in ranked order): compare it against all log entries where `date` is within the last 14 calendar days (calculate from today's date).
   - Matching rule: a candidate matches a log entry if the candidate's normalized label shares 2+ significant words with the log entry's `topic` field (ignore common words: "the", "a", "of", "in", "and", "new", "study", "scientists", "researchers", "find", "show", "reveal", "discover", numbers, articles).
   - If matched: skip this candidate. Print to terminal: `Skipped candidate '[topic]' — covered on [date] (14-day diversity window).`
   - If not matched: this candidate is eligible. Use it.
   - If ALL candidates are skipped: expand to the next-best ranked candidates from the RSS scan (candidates 4-8 if available). If still all covered, widen the match window — accept any candidate not covered in the last 7 days (halve the window). Log to terminal: `All top candidates recently covered — widened diversity window to 7 days.`

7. Present the top eligible candidate to the user:
   `Today's topic: [topic label] (covered by [Feed1], [Feed2]). [One sentence of context from the lead article.] Proceed? (y/n)`

8. If the user confirms: proceed to Step 2 with this topic.

9. If the user rejects: present candidates #2 and #3 in the same format. Ask: `Alternatively: (1) [topic2] or (2) [topic3]. Choose 1, 2, or type a different topic.`

10. If the user rejects all or provides a manual topic: use the provided topic. Apply the dedup check (warn but proceed — see Case B).

**CASE B: `$ARGUMENTS` is not empty (manual topic)**

1. Read `output/topic-log.json`. If the file does not exist, treat as empty array.
2. Check if the provided topic matches any entry in the last 14 days (same 2+ significant words matching rule as Case A).
3. If matched: print warning `Note: [topic] was covered on [date]. Proceeding anyway.` — do NOT block.
4. Show confirmation: `Topic: [topic]. Today's date: [date]. Proceed? (y/n)`
5. If confirmed: proceed to Step 2.
6. If rejected: ask for alternative topic input.

**CASE C: User rejection in auto-discovery (handled inline in Case A steps 9-10)**

No separate section needed — Case A steps 9-10 cover this.

IMPORTANT structural notes:
- Each Case must end with "Then proceed to Step 2."
- The three cases must be clearly labeled with `### Case A:`, `### Case B:`, `### Case C:` subheadings so Claude unambiguously selects the right branch.
- Do NOT delete the `Generate an Instagram science carousel for: $ARGUMENTS` line at the top of SKILL.md.
- Do NOT modify any other Step (2, 3, 4, 5, 6, 7) in this task.
  </action>
  <verify>
    <automated>grep -c "auto-discovery" .claude/skills/science/SKILL.md | grep -q "[1-9]" && grep -c "topic-log.json" .claude/skills/science/SKILL.md | grep -q "[1-9]" && grep -c "Case A:" .claude/skills/science/SKILL.md | grep -q "[1-9]" && grep -c "Case B:" .claude/skills/science/SKILL.md | grep -q "[1-9]" && grep -c "14-day" .claude/skills/science/SKILL.md | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md contains `## Step 1: Discover or confirm topic` heading
    - SKILL.md Step 1 contains `### Case A:` subheading with auto-discovery instructions
    - SKILL.md Step 1 contains `### Case B:` subheading with manual topic + dedup warning
    - SKILL.md Step 1 contains the string `output/topic-log.json` (dedup check)
    - SKILL.md Step 1 contains `14 calendar days` or `14-day` (diversity window)
    - SKILL.md Step 1 contains all 4 RSS feed URLs: `sciencedaily.com/rss/all.xml`, `phys.org/rss-feed/science-news/`, `nature.com/nature.rss`, `site:arstechnica.com/science`
    - SKILL.md Step 1 contains `2+ significant words` (matching rule)
    - SKILL.md Step 1 contains `Skipped candidate` (skip log message)
    - SKILL.md Step 1 contains `Proceed?` (confirmation prompt)
    - SKILL.md Step 1 contains `Today's topic:` (auto-discovery confirmation format)
    - SKILL.md Step 1 contains the WebSearch fallback: `trending science news today`
    - SKILL.md retains `## Step 2:` through `## Step 7:` unchanged
    - SKILL.md retains `Generate an Instagram science carousel for: $ARGUMENTS` on line 9
  </acceptance_criteria>
  <done>Step 1 fully rewritten with three-branch decision table covering auto-discovery, manual topic with dedup, and rejection flow. All 4 RSS feeds referenced. 14-day dedup with 2+ significant words matching rule present. Confirmation UX matches CONTEXT.md spec.</done>
</task>

<task type="auto">
  <name>Task 2: Extend SKILL.md Step 6 with topic-log.json append and Step 7 with log line</name>
  <files>.claude/skills/science/SKILL.md</files>
  <read_first>
    - .claude/skills/science/SKILL.md (current Step 6 and Step 7 — read AFTER Task 1 has modified Step 1)
    - .planning/phases/04-validation-and-auto-topic/04-RESEARCH.md (Pattern 4: topic-log.json append, Pattern 6: Step 7 extension, Pitfall 2: overwrite prevention)
  </read_first>
  <action>
Extend Step 6 and Step 7 in SKILL.md. Do NOT modify Step 1 (already done in Task 1) or any other step.

**Step 6 extension — append after the existing "Write the full carousel package to `output/YYYY-MM-DD-[slug].md`" instruction:**

Add a new subsection `### Topic log update` after the existing Step 6 content with the following instruction text:

```
After writing the output file, update the topic diversity log:

1. Read `output/topic-log.json` using the Read tool. If the file does not exist, start with an empty array `[]`.
2. Append a new entry to the array:
   ```json
   {
     "date": "YYYY-MM-DD",
     "topic": "[topic label]",
     "field": "[Field value from the output file header]",
     "slug": "[slug used in the output filename]"
   }
   ```
   - `date`: today's date in YYYY-MM-DD format
   - `topic`: the confirmed topic label (normalized 2-5 words, as used in Step 1)
   - `field`: extract from the `**Field:**` line in the generated output
   - `slug`: the same slug used in the output filename
3. Write the updated array back to `output/topic-log.json` using the Write tool.

CRITICAL: You must Read the existing file first, then append to the array, then Write the full updated array. Do NOT write only the new entry — this would destroy all previous log history.
```

**Step 7 extension — add a new line to the terminal summary format:**

The current Step 7 terminal summary template is:
```
Generating carousel for: [Topic]...
Generated: [Topic]
   Slides: N | Sources: N | Field: [Field]
   -> output/YYYY-MM-DD-[slug].md
```

Add one new line at the end:
```
   Topic log: updated (output/topic-log.json)
```

So the full Step 7 template becomes:
```
Generating carousel for: [Topic]...
Generated: [Topic]
   Slides: N | Sources: N | Field: [Field]
   -> output/YYYY-MM-DD-[slug].md
   Topic log: updated (output/topic-log.json)
```
  </action>
  <verify>
    <automated>grep -c "topic-log.json" .claude/skills/science/SKILL.md | xargs test 3 -le && grep -q "Topic log: updated" .claude/skills/science/SKILL.md && grep -q "Read.*topic-log.json" .claude/skills/science/SKILL.md && grep -q "CRITICAL.*Read the existing file first" .claude/skills/science/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md Step 6 contains `### Topic log update` subsection
    - SKILL.md Step 6 contains `Read \`output/topic-log.json\`` instruction
    - SKILL.md Step 6 contains `Write the updated array back to \`output/topic-log.json\``
    - SKILL.md Step 6 contains `CRITICAL: You must Read the existing file first` (overwrite prevention)
    - SKILL.md Step 6 contains the JSON schema with `date`, `topic`, `field`, `slug` fields
    - SKILL.md Step 7 terminal summary contains `Topic log: updated (output/topic-log.json)` line
    - SKILL.md `topic-log.json` appears at least 3 times total (Step 1 dedup, Step 6 append, Step 7 summary)
    - Steps 2-5 remain unchanged
  </acceptance_criteria>
  <done>Step 6 extended with topic-log.json read-append-write logic including overwrite prevention warning. Step 7 terminal summary extended with topic log update line. The complete TOPIC-01 and TOPIC-02 instruction set is now in SKILL.md.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full auto-topic and diversity tracking integration:

1. `grep -c "Case A:" .claude/skills/science/SKILL.md` returns 1 (auto-discovery branch exists)
2. `grep -c "Case B:" .claude/skills/science/SKILL.md` returns 1 (manual topic branch exists)
3. `grep -c "topic-log.json" .claude/skills/science/SKILL.md` returns 3+ (Step 1 dedup, Step 6 append, Step 7 summary)
4. `grep -c "14" .claude/skills/science/SKILL.md` confirms 14-day window reference exists
5. `grep -c "Topic log: updated" .claude/skills/science/SKILL.md` returns 1 (Step 7 line)
6. `grep -c "## Step 1:" .claude/skills/science/SKILL.md` returns 1 (Step 1 exists, not duplicated)
7. `grep -c "## Step 7:" .claude/skills/science/SKILL.md` returns 1 (Step 7 exists, not duplicated)
8. All Steps 2-5 headers still present and unmodified
</verification>

<success_criteria>
- SKILL.md Step 1 has a three-branch decision table: auto-discovery (Case A), manual with dedup (Case B), rejection alternatives (inline in Case A)
- Auto-discovery scans 4 RSS feeds in parallel, extracts candidates, ranks by cross-feed frequency
- Dedup checks output/topic-log.json with 14-day window and 2+ significant words matching
- Step 6 appends to topic-log.json using explicit Read-then-Write pattern
- Step 7 includes topic log update confirmation line
- No other steps (2, 3, 4, 5) are modified
</success_criteria>

<output>
After completion, create `.planning/phases/04-validation-and-auto-topic/04-01-SUMMARY.md`
</output>
