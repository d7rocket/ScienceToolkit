---
phase: 04-validation-and-auto-topic
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - .claude/skills/science/SKILL.md
autonomous: true
requirements:
  - TOPIC-01
  - TOPIC-02

must_haves:
  truths:
    - "After Step 5 generates content, 5 format checks run before writing to disk"
    - "A clean output produces 'Validation: PASS' in the Step 7 terminal summary"
    - "An output with violations still gets written to disk but has a warning section prepended at the very top"
    - "The validation warning uses GitHub-flavored Markdown callout syntax (> [!WARNING])"
    - "Caption length check counts characters in caption text only, not the ## Caption heading"
    - "Hashtag check counts only standalone # tokens in the Hashtags section"
  artifacts:
    - path: ".claude/skills/science/SKILL.md"
      provides: "Step 5.5 validation step with 5 hard format checks"
      contains: "Step 5.5"
    - path: ".claude/skills/science/SKILL.md"
      provides: "Step 6 extended with validation warning prepend logic"
      contains: "[!WARNING]"
    - path: ".claude/skills/science/SKILL.md"
      provides: "Step 7 extended with validation pass/fail line"
      contains: "Validation:"
  key_links:
    - from: "SKILL.md Step 5.5"
      to: "SKILL.md Step 6"
      via: "violations list passed from validation to write step"
      pattern: "violations"
    - from: "SKILL.md Step 5.5 checks"
      to: "examples/output-sample.md"
      via: "format rules derived from canonical output structure"
      pattern: "## Slide|## Caption|## Hashtags|## Sources"
    - from: "SKILL.md Step 7"
      to: "SKILL.md Step 5.5"
      via: "validation_status variable set by Step 5.5"
      pattern: "Validation: PASS|FAIL"
---

<objective>
Add post-generation format validation to the science skill.

Purpose: Catch mechanical format violations (caption too long, wrong hashtag count, missing slide labels, incomplete citations) before the user reviews the output. Violations are warned, not blocked -- the file is always written. This completes the "never outputs a carousel that needs manual format fixes" success criterion from the roadmap.

Output: SKILL.md gains Step 5.5 (validation), Step 6 extended (warning prepend + topic log — building on Plan 01), Step 7 extended (validation line added to terminal summary).
</objective>

<execution_context>
@/home/deveshd/.claude/get-shit-done/workflows/execute-plan.md
@/home/deveshd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-validation-and-auto-topic/04-CONTEXT.md
@.planning/phases/04-validation-and-auto-topic/04-RESEARCH.md
@.planning/phases/04-validation-and-auto-topic/04-01-SUMMARY.md
@.claude/skills/science/SKILL.md
@examples/output-sample.md

<interfaces>
<!-- Key structure the executor needs -->

From examples/output-sample.md — the format contract that validation checks against:
```markdown
# [Topic Title]
**Date:** YYYY-MM-DD | **Field:** [Field] | **Sources:** N
---
## Slide 1: [Title]
...
## Slide N: [Title]
...
---
## Caption
[caption text — this is what CHECK 1 measures, excluding the heading]
---
## Hashtags
[5 hashtags — this is what CHECK 2 counts]
---
## Sources
[numbered citations — this is what CHECK 5 inspects]
---
## Images
...
```

From SKILL.md Step 5 Rule 3 (caption ceiling already enforced as self-check):
- Caption hard ceiling is 2100 characters
- Step 5.5 re-validates this mechanically (defense in depth)

From SKILL.md Step 7 (after Plan 01 extends it):
```
Generating carousel for: [Topic]...
Generated: [Topic]
   Slides: N | Sources: N | Field: [Field]
   -> output/YYYY-MM-DD-[slug].md
   Topic log: updated (output/topic-log.json)
```
Plan 02 adds a `Validation:` line between the file path line and the topic log line.

The 5 validation checks (from CONTEXT.md locked decisions):
1. Caption <= 2100 characters (text only, not heading)
2. Exactly 5 hashtags
3. 5-7 slides
4. All slides have `## Slide N: [Title]` labels
5. Every citation has peer-review label + URL line
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Step 5.5 validation and extend Steps 6-7 with validation reporting</name>
  <files>.claude/skills/science/SKILL.md</files>
  <read_first>
    - .claude/skills/science/SKILL.md (read AFTER Plan 01 completes — need current state with auto-discovery Step 1 and topic-log Step 6/7)
    - examples/output-sample.md (canonical format — validation rules check against this structure)
    - .planning/phases/04-validation-and-auto-topic/04-RESEARCH.md (Pattern 3: validation checks, output handling, Pitfall 3: section boundary precision, Pitfall 5: warning placement)
    - .planning/phases/04-validation-and-auto-topic/04-CONTEXT.md (locked decisions: hard format rules only, write-and-warn not write-and-block, no content quality re-checks)
  </read_first>
  <action>
Make three targeted insertions/edits in SKILL.md. Do NOT modify Steps 1-5 (already complete from Plan 01 and Phase 3).

**INSERTION 1: Add new Step 5.5 between Step 5 and Step 6**

Insert a new section `## Step 5.5: Validate format` immediately after the end of Step 5 (after the section ordering code block) and before `## Step 6:`. The full instruction text:

```
## Step 5.5: Validate format

After generating the carousel output in Step 5, run these 5 mechanical checks before writing to disk. Collect ALL violations into a list — do not stop at the first one. Do NOT re-check content quality rules (cliff-hangers, keyword placement, glosses) — Phase 3 self-checks in Step 5 already handle those.

### CHECK 1 — Caption length

Count the total characters in the Caption section text. "Caption section text" means everything between the `## Caption` heading and the next `---` divider, EXCLUDING the `## Caption` heading line itself and any leading/trailing blank lines.

Violation if character count > 2100.
Violation message: `Caption exceeds 2100 characters (actual: N chars).`

### CHECK 2 — Hashtag count

Count all standalone tokens beginning with `#` that appear in the Hashtags section (between the `## Hashtags` heading and the next `---` divider). A standalone hashtag token has no spaces within it (e.g., `#ScienceExplained` is one token). Do NOT count `#` symbols from slide headings, image URLs, or other sections.

Violation if count is not exactly 5.
Violation message: `Hashtag count is N — must be exactly 5.`

### CHECK 3 — Slide count

Count all headings in the generated output that match the pattern `## Slide N:` where N is a number.

Violation if count < 5 or count > 7.
Violation message: `Slide count is N — must be between 5 and 7.`

### CHECK 4 — Slide label format

For each slide heading found in CHECK 3, verify it follows the pattern `## Slide N: [Title]` where N is a sequential number and [Title] is a non-empty descriptive title after the colon. A heading like `## Slide 3:` (no title) or `## Slide Three: Title` (non-numeric) is a violation.

Violation message: `Slide N heading is malformed or missing title.`

### CHECK 5 — Citation completeness

In the Sources section (between `## Sources` and the next `---` divider), for each numbered citation entry, verify:
  (a) A line is present containing one of: `[Published in:`, `[Preprint`, or `[News article]`
  (b) A line beginning with `URL:` is present

Violation for any citation missing either field.
Violation message: `Citation N is missing [peer-review label / URL line].` (specify which is missing)

### Validation result

If the violations list is empty: record `validation_status = "PASS"` for use in Steps 6 and 7.

If the violations list is non-empty: record `validation_status = "FAIL (N violations)"` and keep the full violations list for Step 6.

Proceed to Step 6 in either case — validation does NOT block writing.
```

**EDIT 2: Extend Step 6 with validation warning prepend**

In Step 6, add a new subsection `### Validation warnings` BEFORE the existing `### Topic log update` subsection (added by Plan 01). The instruction text:

```
### Validation warnings

If `validation_status` from Step 5.5 is "PASS": skip this subsection — write the output file normally.

If `validation_status` is "FAIL": prepend the following block at the very top of the output file, BEFORE the `# [Topic Title]` heading. This must be the first content in the file:

> [!WARNING]
> ## Validation Warnings
> The following format violations were detected in this output. Review before posting.
>
> - [violation message 1]
> - [violation message 2]
> (list all violation messages from Step 5.5)
>
> ---

Then write the rest of the carousel content (# [Topic Title] heading and everything after it) immediately below the warning block.
```

**EDIT 3: Extend Step 7 terminal summary with validation line**

In Step 7, add a `Validation:` line to the terminal summary template. Insert it between the file path line (`-> output/...`) and the topic log line (`Topic log: updated...`). The full updated template:

```
Generating carousel for: [Topic]...
Generated: [Topic]
   Slides: N | Sources: N | Field: [Field]
   -> output/YYYY-MM-DD-[slug].md
   Validation: PASS
   Topic log: updated (output/topic-log.json)
```

Or when violations exist:
```
   Validation: FAIL — N violations (see file header)
```

Replace the literal `PASS` or `FAIL — N violations` based on `validation_status` from Step 5.5.
  </action>
  <verify>
    <automated>grep -q "## Step 5.5: Validate format" .claude/skills/science/SKILL.md && grep -q "CHECK 1" .claude/skills/science/SKILL.md && grep -q "CHECK 2" .claude/skills/science/SKILL.md && grep -q "CHECK 3" .claude/skills/science/SKILL.md && grep -q "CHECK 4" .claude/skills/science/SKILL.md && grep -q "CHECK 5" .claude/skills/science/SKILL.md && grep -q "\[!WARNING\]" .claude/skills/science/SKILL.md && grep -q "Validation:" .claude/skills/science/SKILL.md && grep -q "validation_status" .claude/skills/science/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md contains `## Step 5.5: Validate format` heading
    - SKILL.md Step 5.5 contains `### CHECK 1 — Caption length` with boundary spec: "EXCLUDING the `## Caption` heading line itself"
    - SKILL.md Step 5.5 contains `### CHECK 2 — Hashtag count` with scope: "in the Hashtags section"
    - SKILL.md Step 5.5 contains `### CHECK 3 — Slide count` with range: "< 5 or count > 7"
    - SKILL.md Step 5.5 contains `### CHECK 4 — Slide label format` with non-empty title requirement
    - SKILL.md Step 5.5 contains `### CHECK 5 — Citation completeness` checking for `[Published in:`, `[Preprint`, `[News article]`, and `URL:` line
    - SKILL.md Step 5.5 contains `validation does NOT block writing`
    - SKILL.md Step 5.5 contains `Do NOT re-check content quality rules` (anti-duplication of Phase 3 checks)
    - SKILL.md Step 6 contains `### Validation warnings` subsection
    - SKILL.md Step 6 contains `> [!WARNING]` GitHub callout syntax
    - SKILL.md Step 6 contains `BEFORE the \`# [Topic Title]\` heading` (placement precision per Pitfall 5)
    - SKILL.md Step 6 still contains `### Topic log update` subsection (from Plan 01, not deleted)
    - SKILL.md Step 7 terminal summary contains `Validation: PASS` and `Validation: FAIL` variants
    - Steps 1-5 remain unchanged from their Plan 01 / Phase 3 state
  </acceptance_criteria>
  <done>Step 5.5 validation with 5 mechanical checks inserted. Step 6 extended with warning prepend logic (before topic log update). Step 7 terminal summary includes validation pass/fail line. Format validation is complete and non-blocking (write-and-warn).</done>
</task>

</tasks>

<verification>
After task completes, verify the full validation integration:

1. `grep -c "## Step 5.5:" .claude/skills/science/SKILL.md` returns 1 (validation step exists)
2. `grep -c "CHECK [1-5]" .claude/skills/science/SKILL.md` returns 5 (all 5 checks present)
3. `grep -c "\[!WARNING\]" .claude/skills/science/SKILL.md` returns 1 (warning block format)
4. `grep -c "Validation:" .claude/skills/science/SKILL.md` returns at least 1 (Step 7 line)
5. `grep -c "validation_status" .claude/skills/science/SKILL.md` returns at least 2 (set in 5.5, used in 6 and 7)
6. Step ordering is correct: Step 5 -> Step 5.5 -> Step 6 -> Step 7
7. Step 6 has both `### Validation warnings` and `### Topic log update` subsections (Plan 01 content preserved)
</verification>

<success_criteria>
- SKILL.md has a Step 5.5 with 5 clearly specified format checks (caption chars, hashtag count, slide count, slide labels, citation completeness)
- Validation is non-blocking: file always gets written, violations are warned not blocked
- Warning section uses `> [!WARNING]` callout and appears before the `# [Topic Title]` heading
- Step 7 terminal summary shows Validation: PASS or FAIL with violation count
- No content quality rules are re-checked (cliff-hangers, keyword gate, glosses are Phase 3's domain)
- Plan 01 additions (Step 1 auto-discovery, Step 6 topic log, Step 7 topic log line) remain intact
</success_criteria>

<output>
After completion, create `.planning/phases/04-validation-and-auto-topic/04-02-SUMMARY.md`
</output>
