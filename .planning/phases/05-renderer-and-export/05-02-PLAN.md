---
phase: 05-renderer-and-export
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - carousel-ui/src/canvas/constants.ts
  - carousel-ui/src/canvas/layouts.ts
  - carousel-ui/src/canvas/renderSlide.ts
  - carousel-ui/src/hooks/useFontReady.ts
  - carousel-ui/src/components/SlideCanvas.tsx
  - carousel-ui/src/components/ThumbnailStrip.tsx
  - carousel-ui/src/App.tsx
  - carousel-ui/src/canvas/__tests__/layouts.test.ts
  - carousel-ui/src/canvas/__tests__/constants.test.ts
  - carousel-ui/src/components/__tests__/ThumbnailStrip.test.tsx
autonomous: true
requirements:
  - RNDR-01
  - RNDR-02
  - RNDR-03
  - RNDR-04
  - RNDR-05
  - RNDR-06

must_haves:
  truths:
    - "After loading a markdown file, all slides are visible as rendered previews in the thumbnail strip on the left"
    - "Clicking a thumbnail in the strip switches the main canvas to that slide"
    - "The main canvas always renders at 1080x1080 — canvas.getWidth() returns 1080 not 2160"
    - "Canvas render is blocked until fonts are confirmed loaded (fontsReady = true) — no canvas mounts before font gate"
    - "The safe zone overlay toggle button shows/hides dashed rectangle overlay on the canvas"
    - "Hook slide shows large title at ~64px in the top portion of the safe zone with body text below"
    - "Body slides show title at 36-44px and body text at 22-26px with a slide number badge"
    - "CTA slide shows takeaway sentence and Follow for daily science drops line"
  artifacts:
    - path: "carousel-ui/src/canvas/constants.ts"
      provides: "CANVAS_SIZE=1080, SAFE_TOP=120, SAFE_BOTTOM=150, SAFE_SIDES=80"
      exports: ["CANVAS_SIZE", "SAFE_TOP", "SAFE_BOTTOM", "SAFE_SIDES"]
    - path: "carousel-ui/src/canvas/layouts.ts"
      provides: "renderHookSlide, renderBodySlide, renderCtaSlide layout functions"
      exports: ["renderHookSlide", "renderBodySlide", "renderCtaSlide"]
    - path: "carousel-ui/src/canvas/renderSlide.ts"
      provides: "Dispatch function that calls correct layout by slide role"
      exports: ["renderSlide"]
    - path: "carousel-ui/src/components/SlideCanvas.tsx"
      provides: "Main 1080x1080 Fabric.js canvas with scale-to-preview"
    - path: "carousel-ui/src/components/ThumbnailStrip.tsx"
      provides: "Left sidebar with one mini Fabric.js canvas per slide"
  key_links:
    - from: "carousel-ui/src/components/SlideCanvas.tsx"
      to: "carousel-ui/src/canvas/renderSlide.ts"
      via: "renderSlide(fabricCanvas, slide, colors) called in useEffect when activeSlide or colors changes"
      pattern: "renderSlide"
    - from: "carousel-ui/src/components/ThumbnailStrip.tsx"
      to: "carousel-ui/src/canvas/renderSlide.ts"
      via: "renderSlide called for each thumbnail canvas when slide data changes"
      pattern: "renderSlide"
    - from: "carousel-ui/src/components/SlideCanvas.tsx"
      to: "carousel-ui/src/store/useCarouselStore.ts"
      via: "useCarouselStore(s => s.fontsReady) gates canvas mount"
      pattern: "fontsReady"
    - from: "carousel-ui/src/canvas/layouts.ts"
      to: "carousel-ui/src/canvas/constants.ts"
      via: "SAFE_TOP, SAFE_BOTTOM, SAFE_SIDES, CANVAS_SIZE imported and used for all object positions"
      pattern: "SAFE_"
---

<objective>
Implement the Fabric.js canvas rendering system: design constants, three role-aware slide layouts, the main SlideCanvas component, the ThumbnailStrip sidebar, and the safe zone overlay toggle.

Purpose: This is the core visual output of the app. Plans 01 and 03 form the bookends; this plan is the canvas engine itself.
Output: A running app where loading a markdown file immediately renders all slides as Fabric.js canvases — full-resolution in the main area and mini-previews in the left sidebar.
</objective>

<execution_context>
@/home/deveshd/.claude/get-shit-done/workflows/execute-plan.md
@/home/deveshd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-renderer-and-export/05-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 that this plan consumes. -->

From carousel-ui/src/types/carousel.ts:

  export interface ColorScheme {
    background: string;
    primaryText: string;
    accent: string;
    highlight: string;
  }

  export type SlideRole = 'hook' | 'body' | 'cta';

  export interface ParsedSlide {
    index: number;    // 1-based
    role: SlideRole;
    title: string;
    body: string;
  }

From carousel-ui/src/store/useCarouselStore.ts:

  interface CarouselStore {
    slides: ParsedSlide[];
    colors: ColorScheme;
    activeSlideIndex: number;
    safezoneVisible: boolean;
    fontsReady: boolean;
    setActiveSlide: (index: number) => void;
    toggleSafezone: () => void;
    setFontsReady: (ready: boolean) => void;
  }

Fabric.js v6 named imports (NOT v5 namespace pattern):
  import { Canvas, FabricText, Textbox, Rect } from 'fabric';
  NOT: import { fabric } from 'fabric'

Canvas initialization with REQUIRED pixelRatio=1:
  const fc = new Canvas(el, { width: 1080, height: 1080, pixelRatio: 1, selection: false, renderOnAddRemove: false });

CSS scale() pattern for preview shrinking:
  Main preview: scale = containerWidth / 1080 (e.g. 540/1080 = 0.5)
  Thumbnails: scale = thumbnailSize / 1080 (e.g. 160/1080 = ~0.148)
  Wrap in: <div style={{ width: containerWidth, height: containerWidth, overflow: 'hidden' }}>
             <div style={{ width: 1080, height: 1080, transform: scale(N), transformOrigin: 'top left' }}>
               <canvas ... />

Safe zone constants (from 05-CONTEXT.md):
  SAFE_TOP = 120    (top boundary in pixels)
  SAFE_BOTTOM = 150 (bottom boundary in pixels, from bottom of canvas)
  SAFE_SIDES = 80   (left and right boundary in pixels)
  CANVAS_SIZE = 1080

Safe zone content area (where slide content lives):
  x start: SAFE_SIDES (80)
  x end: CANVAS_SIZE - SAFE_SIDES (1000)
  y start: SAFE_TOP (120)
  y end: CANVAS_SIZE - SAFE_BOTTOM (930)
  content width: 920px (1080 - 80 - 80)
  content height: 810px (1080 - 120 - 150)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create canvas constants, layout functions, and renderSlide dispatcher</name>
  <read_first>
    - carousel-ui/src/types/carousel.ts (ParsedSlide, ColorScheme, SlideRole types)
    - .planning/phases/05-renderer-and-export/05-CONTEXT.md (slide layout decisions: hook slide 64px title top 40% of safe zone, body slides 36-44px title / 22-26px body / slide number badge, CTA slide takeaway + follow CTA, safe zone 120/150/80)
    - .planning/REQUIREMENTS.md (RNDR-03 layout spec, RNDR-04 constant margins, RNDR-05 safe zone overlay, QUAL-02 typography sizes)
    - .planning/phases/05-renderer-and-export/05-RESEARCH.md (Fabric.js v6 named imports, pitfall 4 on import syntax, pitfall 3 on pixelRatio)
  </read_first>
  <files>
    carousel-ui/src/canvas/constants.ts,
    carousel-ui/src/canvas/layouts.ts,
    carousel-ui/src/canvas/renderSlide.ts,
    carousel-ui/src/canvas/__tests__/layouts.test.ts,
    carousel-ui/src/canvas/__tests__/constants.test.ts
  </files>
  <behavior>
    - CANVAS_SIZE === 1080, SAFE_TOP === 120, SAFE_BOTTOM === 150, SAFE_SIDES === 80
    - renderHookSlide places a FabricText title object with fontSize 64 inside the safe zone
    - renderBodySlide places a title FabricText at 36-44px and a Textbox body at 22-26px, plus a slide number badge
    - renderCtaSlide places takeaway text and "Follow for daily science drops" CTA text
    - All layout functions clear the canvas before placing new objects (canvas.clear() or canvas.remove all)
    - Safe zone overlay Rect objects have evented: false and selectable: false
    - After each layout function, canvas.renderAll() is called
  </behavior>
  <action>
FILE 1: carousel-ui/src/canvas/constants.ts

  export const CANVAS_SIZE = 1080;
  export const SAFE_TOP = 120;
  export const SAFE_BOTTOM = 150;
  export const SAFE_SIDES = 80;

  // Derived content area bounds — use these in layout functions
  export const CONTENT_X = SAFE_SIDES;                        // 80
  export const CONTENT_Y = SAFE_TOP;                          // 120
  export const CONTENT_WIDTH = CANVAS_SIZE - SAFE_SIDES * 2;  // 920
  export const CONTENT_HEIGHT = CANVAS_SIZE - SAFE_TOP - SAFE_BOTTOM; // 810
  export const CONTENT_BOTTOM = CANVAS_SIZE - SAFE_BOTTOM;    // 930

FILE 2: carousel-ui/src/canvas/layouts.ts

Import from Fabric.js v6 with NAMED imports:
  import { Canvas, FabricText, Textbox, Rect } from 'fabric';

CRITICAL: Do NOT use import { fabric } from 'fabric' — that is v5 syntax and will throw at runtime.

Implement three layout functions. Each function signature:
  function renderHookSlide(canvas: Canvas, slide: ParsedSlide, colors: ColorScheme): void
  function renderBodySlide(canvas: Canvas, slide: ParsedSlide, colors: ColorScheme): void
  function renderCtaSlide(canvas: Canvas, slide: ParsedSlide, colors: ColorScheme): void

Each function must:
1. Call canvas.clear() first (removes all objects including previous slide)
2. Add a background rectangle filling the full 1080x1080 canvas with colors.background fill
3. Place text objects within the safe zone using CONTENT_X, CONTENT_Y, CONTENT_WIDTH, CONTENT_HEIGHT constants
4. Call canvas.renderAll() at the end

HOOK SLIDE layout (renderHookSlide):
- Background rect: left=0, top=0, width=1080, height=1080, fill=colors.background, selectable=false, evented=false
- Title: FabricText with slide.title, fontFamily='Inter', fontWeight='700', fontSize=64, fill=colors.primaryText
  - left=CONTENT_X (80), top=CONTENT_Y (120), width=CONTENT_WIDTH (920), textAlign='center'
  - This fills roughly the top 40% of the safe zone — the hook slide is whitespace-dominant
- Body text: FabricText with slide.body, fontFamily='Inter', fontWeight='400', fontSize=28, fill=colors.primaryText
  - left=CONTENT_X, top=CONTENT_Y + 300 (below title), width=CONTENT_WIDTH, textAlign='center'
  - Opacity 0.8 for visual hierarchy
- Both objects: selectable=false, evented=false

BODY SLIDE layout (renderBodySlide):
- Background rect: same as hook slide
- Slide number badge: small rounded rectangle in top-right of safe zone with slide.index text
  - Badge position: left = CONTENT_X + CONTENT_WIDTH - 80, top = CONTENT_Y - 10
  - Badge: Rect with fill=colors.accent, width=56, height=32, rx=8, ry=8, selectable=false, evented=false
  - Badge text: FabricText(String(slide.index), fontSize=18, fill='#ffffff', fontWeight='700')
    - Centered over the badge rect
- Title: FabricText with slide.title, fontFamily='Inter', fontWeight='700', fontSize=40, fill=colors.primaryText
  - left=CONTENT_X, top=CONTENT_Y + 30, width=CONTENT_WIDTH
- Body: Textbox with slide.body, fontFamily='Inter', fontWeight='400', fontSize=24, fill=colors.primaryText
  - left=CONTENT_X, top=CONTENT_Y + 140, width=CONTENT_WIDTH
  - Opacity 0.85, lineHeight=1.5
- All objects: selectable=false, evented=false

CTA SLIDE layout (renderCtaSlide):
- Background rect: same as hook slide
- Accent line: horizontal Rect as a decorative separator
  - left=CONTENT_X, top=CANVAS_SIZE/2 - 40, width=120, height=4, fill=colors.accent, selectable=false, evented=false
- Takeaway text: FabricText with slide.body, fontFamily='Inter', fontWeight='700', fontSize=36, fill=colors.primaryText
  - left=CONTENT_X, top=CANVAS_SIZE/2 - 20, width=CONTENT_WIDTH, textAlign='left'
- CTA line: FabricText('Follow for daily science drops', fontFamily='Inter', fontWeight='400', fontSize=22, fill=colors.accent)
  - left=CONTENT_X, top=CANVAS_SIZE/2 + 120, width=CONTENT_WIDTH

SAFE ZONE OVERLAY function (export separately):
  export function renderSafezoneOverlay(canvas: Canvas, visible: boolean): void

This function adds or removes the safe zone overlay Rect object. Use a named object approach:
- Create a Rect with: left=SAFE_SIDES, top=SAFE_TOP, width=CONTENT_WIDTH, height=CONTENT_HEIGHT
- stroke=colors.accent or '#6C5CE7' (hardcode for overlay), fill='', strokeWidth=2, strokeDashArray=[8,4]
- evented=false, selectable=false, visible=visible
- Tag the object: set rect.set('name', 'safezone-overlay') or use a module-level variable to track it
- On re-call: find existing overlay by name and set its visible property, then canvas.renderAll()

FILE 3: carousel-ui/src/canvas/renderSlide.ts

Dispatcher that routes to the correct layout function based on slide.role:

  import { Canvas } from 'fabric';
  import { ParsedSlide, ColorScheme } from '../types/carousel';
  import { renderHookSlide, renderBodySlide, renderCtaSlide } from './layouts';

  export function renderSlide(canvas: Canvas, slide: ParsedSlide, colors: ColorScheme): void {
    switch (slide.role) {
      case 'hook': return renderHookSlide(canvas, slide, colors);
      case 'body': return renderBodySlide(canvas, slide, colors);
      case 'cta':  return renderCtaSlide(canvas, slide, colors);
    }
  }

FILE 4: carousel-ui/src/canvas/__tests__/constants.test.ts

Tests:
1. CANVAS_SIZE === 1080
2. SAFE_TOP === 120
3. SAFE_BOTTOM === 150
4. SAFE_SIDES === 80
5. CONTENT_WIDTH === 920 (1080 - 80 - 80)
6. CONTENT_HEIGHT === 810 (1080 - 120 - 150)

FILE 5: carousel-ui/src/canvas/__tests__/layouts.test.ts

Mock the Canvas class from fabric (vi.mock('fabric')). Create a mock canvas with:
- clear: vi.fn()
- add: vi.fn()
- renderAll: vi.fn()
- getObjects: vi.fn().mockReturnValue([])

Tests:
1. renderHookSlide: canvas.clear() is called before canvas.add()
2. renderHookSlide: canvas.renderAll() is called
3. renderBodySlide: canvas.add() is called at least 4 times (bg + title + body + badge)
4. renderCtaSlide: canvas.renderAll() is called
5. renderSlide dispatches to renderHookSlide for role='hook', renderBodySlide for role='body', renderCtaSlide for role='cta'
  </action>
  <verify>
    <automated>cd /home/deveshd/Documents/ScienceToolkit/carousel-ui && npm run test -- --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "CANVAS_SIZE = 1080" carousel-ui/src/canvas/constants.ts returns a match
    - grep "SAFE_TOP = 120" carousel-ui/src/canvas/constants.ts returns a match
    - grep "SAFE_BOTTOM = 150" carousel-ui/src/canvas/constants.ts returns a match
    - grep "import { Canvas, FabricText" carousel-ui/src/canvas/layouts.ts returns a match (NOT "import { fabric }")
    - grep "canvas.clear()" carousel-ui/src/canvas/layouts.ts returns at least 3 matches (one per layout function)
    - grep "canvas.renderAll()" carousel-ui/src/canvas/layouts.ts returns at least 3 matches
    - npm run test -- --run exits 0 with 0 failures
  </acceptance_criteria>
  <done>
    - Constants match spec exactly: 1080, 120, 150, 80
    - All three layout functions clear canvas, build objects, and call renderAll
    - renderSlide correctly dispatches by role
    - Test suite still fully green
  </done>
</task>

<task type="auto">
  <name>Task 2: Build SlideCanvas and ThumbnailStrip components, update App.tsx layout</name>
  <read_first>
    - carousel-ui/src/canvas/constants.ts (CANVAS_SIZE just created)
    - carousel-ui/src/canvas/renderSlide.ts (renderSlide function signature just created)
    - carousel-ui/src/canvas/layouts.ts (renderSafezoneOverlay function just created)
    - carousel-ui/src/store/useCarouselStore.ts (activeSlideIndex, fontsReady, safezoneVisible, slides, colors, setActiveSlide, toggleSafezone)
    - .planning/phases/05-renderer-and-export/05-CONTEXT.md (thumbnail strip position: left sidebar vertical, each thumbnail is a live Fabric.js canvas, CSS scale from 1080)
    - .planning/phases/05-renderer-and-export/05-RESEARCH.md (Pattern 1: Fabric.js in React useRef+useEffect, Pattern 2: CSS scale() preview shrinking, pitfall 2: font gate, pitfall 3: pixelRatio)
  </read_first>
  <files>
    carousel-ui/src/components/SlideCanvas.tsx,
    carousel-ui/src/components/ThumbnailStrip.tsx,
    carousel-ui/src/App.tsx,
    carousel-ui/src/components/__tests__/ThumbnailStrip.test.tsx
  </files>
  <action>
FILE 1: carousel-ui/src/components/SlideCanvas.tsx

The main 1080x1080 Fabric.js canvas. Shrinks visually to fit the preview area via CSS scale().

Props: none (reads from store directly)
Store reads: slides, colors, activeSlideIndex, safezoneVisible, fontsReady

Key behaviors:
- Canvas MUST NOT initialize until fontsReady === true (gate from font loading)
- Canvas initialized with: new Canvas(el, { width: 1080, height: 1080, pixelRatio: 1, selection: false, renderOnAddRemove: false })
  pixelRatio: 1 is MANDATORY — never omit or override with devicePixelRatio
- On mount: initialize Fabric.js Canvas, dispose on unmount (return () => fc.dispose())
- When activeSlideIndex or colors change: call renderSlide(fabricCanvas, slides[activeSlideIndex], colors)
- When safezoneVisible changes: call renderSafezoneOverlay(fabricCanvas, safezoneVisible)
- Safe zone toggle button rendered outside/above the canvas: onClick calls toggleSafezone from store

Preview scaling: The preview container should fit within the available right-side area.
Use a fixed preview size of 540px wide. Scale = 540 / 1080 = 0.5.

Layout structure:
  <div className="flex flex-col items-center">
    {/* Safe zone toggle */}
    <button onClick={toggleSafezone}>
      {safezoneVisible ? 'Hide' : 'Show'} Safe Zone
    </button>
    {/* Scaled canvas container */}
    <div style={{ width: 540, height: 540, overflow: 'hidden', position: 'relative' }}>
      <div style={{ width: 1080, height: 1080, transform: 'scale(0.5)', transformOrigin: 'top left' }}>
        <canvas ref={canvasElRef} />
      </div>
    </div>
  </div>

When fontsReady is false: render a loading indicator ("Loading fonts...") instead of the canvas container.

When slides array is empty: render a message ("Load a markdown file to begin") instead.

FILE 2: carousel-ui/src/components/ThumbnailStrip.tsx

Left sidebar with one mini Fabric.js canvas per slide. Each thumbnail:
- Is a live Fabric.js Canvas instance (not a PNG snapshot)
- Renders at full 1080x1080 internally
- Displayed via CSS scale: thumbnail display size is 160px, so scale = 160/1080
- Container: div with width=160, height=160, overflow=hidden
- Inner: div with width=1080, height=1080, transform scale(160/1080), transformOrigin top left
- onClick: calls setActiveSlide(slide.index - 1) from store

Each thumbnail canvas:
- Initialized with: new Canvas(el, { width: 1080, height: 1080, pixelRatio: 1, selection: false, renderOnAddRemove: false })
- On mount: renderSlide(thumbCanvas, slide, colors)
- Active slide highlighted: border ring (ring-2 ring-indigo-400 or CSS outline) on the active thumbnail
- Dispose on unmount

ThumbnailStrip renders null when slides array is empty.

Component structure:
  export function ThumbnailStrip() using useCarouselStore for slides, colors, activeSlideIndex, setActiveSlide

For each slide, render a Thumbnail child component (can be inline or a separate component in the same file):
  - Holds its own useRef and useEffect for Fabric.js lifecycle
  - Accepts: slide, colors, isActive, onClick
  - Applies active highlight border when isActive is true

FILE 3: carousel-ui/src/App.tsx

Update the App.tsx from Plan 01 to wire in the real components. Replace the placeholder div with:
- A two-column flex layout: left sidebar (ThumbnailStrip) + main content area (SlideCanvas)
- ThumbnailStrip on the left, fixed width ~180px with some padding
- SlideCanvas takes the remaining space (flex-1)
- Layout only shows when slides.length > 0 (DropZone handles the empty state)

Updated App.tsx structure:
  import { useEffect } from 'react';
  import { DropZone } from './components/DropZone';
  import { MetaBar } from './components/MetaBar';
  import { SlideCanvas } from './components/SlideCanvas';
  import { ThumbnailStrip } from './components/ThumbnailStrip';
  import { useCarouselStore } from './store/useCarouselStore';

  export default function App() {
    const { meta, slides, setFontsReady } = useCarouselStore();

    useEffect(() => {
      document.fonts.ready.then(() => setFontsReady(true));
    }, [setFontsReady]);

    return (
      <div className="min-h-screen bg-neutral-950 text-white flex flex-col">
        {meta && <MetaBar />}
        <DropZone>
          {slides.length > 0 ? (
            <div className="flex flex-1 overflow-hidden">
              <aside className="w-44 flex-shrink-0 border-r border-neutral-800 overflow-y-auto bg-neutral-900">
                <ThumbnailStrip />
              </aside>
              <main className="flex-1 flex items-start justify-center p-6 overflow-auto">
                <SlideCanvas />
              </main>
            </div>
          ) : (
            <div className="flex-1 flex items-center justify-center text-neutral-600 text-sm">
              Load a markdown file to begin
            </div>
          )}
        </DropZone>
      </div>
    );
  }

FILE 4: carousel-ui/src/components/__tests__/ThumbnailStrip.test.tsx

Mock Fabric.js Canvas: vi.mock('fabric', () => ({ Canvas: vi.fn().mockImplementation(() => ({ add: vi.fn(), clear: vi.fn(), renderAll: vi.fn(), dispose: vi.fn(), getObjects: vi.fn().mockReturnValue([]) })) }))

Mock useCarouselStore to return 3 slides with role hook/body/cta and activeSlideIndex=0.

Tests:
1. ThumbnailStrip renders 3 thumbnail containers when slides has 3 items
2. ThumbnailStrip renders null (or empty) when slides is empty
3. Clicking a thumbnail calls setActiveSlide with the correct index
4. The active thumbnail has a visual distinction (aria-selected or a class containing "ring" or "active")
  </action>
  <verify>
    <automated>cd /home/deveshd/Documents/ScienceToolkit/carousel-ui && npm run test -- --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "pixelRatio: 1" carousel-ui/src/components/SlideCanvas.tsx returns a match (CRITICAL — must be present)
    - grep "fontsReady" carousel-ui/src/components/SlideCanvas.tsx returns a match (font gate check)
    - grep "fc.dispose()" carousel-ui/src/components/SlideCanvas.tsx returns a match (cleanup on unmount)
    - grep "pixelRatio: 1" carousel-ui/src/components/ThumbnailStrip.tsx returns a match
    - grep "renderSlide" carousel-ui/src/components/SlideCanvas.tsx returns a match
    - grep "renderSlide" carousel-ui/src/components/ThumbnailStrip.tsx returns a match
    - grep "import { Canvas" carousel-ui/src/components/SlideCanvas.tsx returns a match (NOT "import { fabric }")
    - npm run test -- --run exits 0 with 0 failures
  </acceptance_criteria>
  <done>
    - Loading output/2026-03-16-crispr-gene-editing.md shows 6 thumbnail canvases in the left sidebar
    - Clicking a thumbnail switches the main canvas to that slide
    - The safe zone toggle button appears and clicking it shows/hides the dashed overlay on the main canvas
    - canvas.getWidth() returns 1080 (not 2160 on retina displays)
    - npm run test -- --run exits 0
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run the full test suite:

  cd /home/deveshd/Documents/ScienceToolkit/carousel-ui && npm run test -- --run --reporter=verbose

All test files must pass:
- src/canvas/__tests__/constants.test.ts (RNDR-04)
- src/canvas/__tests__/layouts.test.ts (RNDR-03, RNDR-05)
- src/components/__tests__/ThumbnailStrip.test.tsx (RNDR-06)
- All Plan 01 tests must still pass (regression check)

Manual spot-check:
  cd /home/deveshd/Documents/ScienceToolkit/carousel-ui && npm run dev
  1. Drop output/2026-03-16-crispr-gene-editing.md into the browser
  2. Verify: 6 thumbnails appear in the left sidebar
  3. Verify: Main canvas shows Slide 1 (CRISPR hook slide) with dark background and Inter font
  4. Click Slide 3 thumbnail — main canvas updates to Slide 3 content
  5. Click safe zone button — dashed overlay appears on main canvas
  6. Click safe zone button again — overlay disappears
</verification>

<success_criteria>
1. npm run test -- --run exits 0 with 0 failures across all test files
2. Loading a real markdown file shows all slides rendered in the thumbnail strip within 2 seconds
3. Main canvas renders at exactly 1080x1080 (pixelRatio: 1 confirmed)
4. Canvas does not render until fonts are confirmed loaded (no Arial fallback on first load)
5. Three slide roles render with distinct layouts: hook (large title, minimal), body (title+text+badge), CTA (takeaway+follow line)
6. Safe zone overlay toggle works: dashed rectangle appears/disappears on click
</success_criteria>

<output>
After completion, create .planning/phases/05-renderer-and-export/05-02-SUMMARY.md following the summary template at @/home/deveshd/.claude/get-shit-done/templates/summary.md
</output>
