---
phase: 05-renderer-and-export
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - carousel-ui/src/export/exportPng.ts
  - carousel-ui/src/export/exportZip.ts
  - carousel-ui/src/components/ExportPanel.tsx
  - carousel-ui/src/App.tsx
  - carousel-ui/src/export/__tests__/exportPng.test.ts
  - carousel-ui/src/export/__tests__/exportZip.test.ts
autonomous: false
requirements:
  - XPRT-01
  - XPRT-02
  - XPRT-03

must_haves:
  truths:
    - "Clicking the single-slide export button on any slide downloads a 1080x1080 PNG file"
    - "Clicking Export All ZIP downloads a .zip file containing one PNG per slide (named slide-01.png, slide-02.png, etc.)"
    - "A progress bar advances from 0% to 100% during ZIP export and resets after download"
    - "Each per-slide export button in the thumbnail strip also works as a fallback download"
    - "Exported PNG files open in an image viewer as 1080x1080 pixels (not 540x540, not 2160x2160)"
    - "Export does not break or produce corrupted output if fonts are loaded and all canvas assets are self-hosted"
  artifacts:
    - path: "carousel-ui/src/export/exportPng.ts"
      provides: "Single-slide PNG export via canvas.toDataURL"
      exports: ["exportSlideAsPng"]
    - path: "carousel-ui/src/export/exportZip.ts"
      provides: "ZIP bundle export using JSZip with progress callback"
      exports: ["exportAllSlidesAsZip"]
    - path: "carousel-ui/src/components/ExportPanel.tsx"
      provides: "Export All ZIP button with progress bar and per-slide download buttons"
  key_links:
    - from: "carousel-ui/src/components/ExportPanel.tsx"
      to: "carousel-ui/src/export/exportZip.ts"
      via: "exportAllSlidesAsZip(canvasRefs, topicSlug, onProgress) called on button click"
      pattern: "exportAllSlidesAsZip"
    - from: "carousel-ui/src/components/ExportPanel.tsx"
      to: "carousel-ui/src/export/exportPng.ts"
      via: "exportSlideAsPng(canvas, filename) called per-slide download button"
      pattern: "exportSlideAsPng"
    - from: "carousel-ui/src/export/exportPng.ts"
      to: "Fabric.js canvas.toDataURL"
      via: "canvas.toDataURL({ format: 'png', multiplier: 1 }) — multiplier:1 is CRITICAL"
      pattern: "toDataURL"
---

<objective>
Build the PNG and ZIP export pipeline, wire it to the ExportPanel component, and harden the drag-drop experience. This is the final delivery plan — after this phase is done the app is shippable.

Purpose: Export fidelity is the core promise of WYSIWYG. This plan closes the loop from canvas render to Instagram-ready PNG.
Output: A fully functional export system with per-slide PNG downloads, a ZIP bundle export with progress indicator, and per-slide fallback buttons in the thumbnail area.
</objective>

<execution_context>
@/home/deveshd/.claude/get-shit-done/workflows/execute-plan.md
@/home/deveshd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-renderer-and-export/05-01-SUMMARY.md
@.planning/phases/05-renderer-and-export/05-02-SUMMARY.md

<interfaces>
<!-- Types and patterns from Plans 01 and 02 that this plan consumes. -->

From carousel-ui/src/types/carousel.ts:
  interface ParsedSlide { index: number; role: SlideRole; title: string; body: string; }
  interface CarouselMeta { title: string; date: string; field: string; slideCount: number; }

From carousel-ui/src/store/useCarouselStore.ts:
  exportProgress: number;   // 0-100, 0 = idle
  exportError: string | null;
  setExportProgress: (pct: number) => void;
  setExportError: (err: string | null) => void;
  meta: CarouselMeta | null;

Fabric.js v6 export pattern (CRITICAL — do not deviate):
  canvas.toDataURL({ format: 'png', multiplier: 1 })
  NEVER pass multiplier > 1 — canvas is already 1080x1080
  NEVER use window.devicePixelRatio in the export path

JSZip pattern:
  import JSZip from 'jszip';
  const zip = new JSZip();
  const folder = zip.folder(topicSlug)!;
  folder.file('slide-01.png', blob);
  const zipBlob = await zip.generateAsync({ type: 'blob' });
  // Trigger download via URL.createObjectURL

Canvas ref passing strategy:
  The ExportPanel needs access to all Fabric.js Canvas instances.
  Use a canvasRegistry: Map<number, Canvas> pattern stored in a module-level variable
  or passed via React ref forwarding.
  Recommended: export a module-level registry from a new file:
    carousel-ui/src/canvas/canvasRegistry.ts
    export const canvasRegistry = new Map<number, import('fabric').Canvas>();
  ThumbnailStrip and SlideCanvas both register their canvas instances into this map.
  ExportPanel reads from canvasRegistry to collect all canvases for ZIP export.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement PNG export, ZIP export with progress, and canvas registry</name>
  <read_first>
    - carousel-ui/src/canvas/constants.ts (CANVAS_SIZE = 1080 — export must match this)
    - carousel-ui/src/store/useCarouselStore.ts (setExportProgress, setExportError, meta.title for ZIP filename)
    - .planning/phases/05-renderer-and-export/05-RESEARCH.md (Pattern 4: PNG Export, Pattern 5: ZIP Bundle Export, pitfall 3 on pixelRatio/multiplier, pitfall 1 on canvas taint)
  </read_first>
  <files>
    carousel-ui/src/canvas/canvasRegistry.ts,
    carousel-ui/src/export/exportPng.ts,
    carousel-ui/src/export/exportZip.ts,
    carousel-ui/src/export/__tests__/exportPng.test.ts,
    carousel-ui/src/export/__tests__/exportZip.test.ts
  </files>
  <behavior>
    - exportSlideAsPng calls canvas.toDataURL({ format: 'png', multiplier: 1 }) — not multiplier: 2, not devicePixelRatio
    - exportSlideAsPng triggers an anchor tag click with the correct download filename
    - exportAllSlidesAsZip calls onProgress callback with values 0 through 100 as slides are processed
    - exportAllSlidesAsZip produces a ZIP blob with exactly N files matching the slide count
    - ZIP files are named slide-01.png, slide-02.png, etc. (zero-padded to 2 digits)
    - canvasRegistry is a Map that can register and retrieve Canvas instances by slide index
  </behavior>
  <action>
FILE 1: carousel-ui/src/canvas/canvasRegistry.ts

A module-level registry used by SlideCanvas and ThumbnailStrip to register their Canvas instances,
and by ExportPanel to collect all canvases for ZIP export:

  import { Canvas } from 'fabric';

  // Registry maps slide index (0-based) to its Fabric.js Canvas instance
  // Thumbnail canvases are registered here — they are always in sync with slide data
  // and are the source of truth for export
  export const canvasRegistry = new Map<number, Canvas>();

  export function registerCanvas(slideIndex: number, canvas: Canvas): void {
    canvasRegistry.set(slideIndex, canvas);
  }

  export function unregisterCanvas(slideIndex: number): void {
    canvasRegistry.delete(slideIndex);
  }

  export function getAllCanvases(): Canvas[] {
    // Return canvases sorted by slide index (0, 1, 2, ...)
    const entries = Array.from(canvasRegistry.entries());
    entries.sort((a, b) => a[0] - b[0]);
    return entries.map(([, canvas]) => canvas);
  }

NOTE: Thumbnail canvases (not the main preview canvas) are used for export.
All thumbnail canvases are always rendered and in sync with slide data.
This avoids needing to iterate through slides and re-render for export.

FILE 2: carousel-ui/src/export/exportPng.ts

  import { Canvas } from 'fabric';

  export function exportSlideAsPng(canvas: Canvas, filename: string): void {
    // multiplier: 1 is MANDATORY — canvas is already 1080x1080
    // NEVER use multiplier > 1 or window.devicePixelRatio
    const dataUrl = canvas.toDataURL({
      format: 'png',
      multiplier: 1,
    });
    const link = document.createElement('a');
    link.href = dataUrl;
    link.download = filename;
    document.body.appendChild(link);
    link.click();
    document.body.removeChild(link);
  }

FILE 3: carousel-ui/src/export/exportZip.ts

  import JSZip from 'jszip';
  import { Canvas } from 'fabric';

  export async function exportAllSlidesAsZip(
    canvases: Canvas[],
    topicSlug: string,
    onProgress: (pct: number) => void,
  ): Promise<void> {
    const zip = new JSZip();
    const folder = zip.folder(topicSlug);
    if (!folder) throw new Error('Failed to create ZIP folder');

    for (let i = 0; i < canvases.length; i++) {
      const dataUrl = canvases[i].toDataURL({ format: 'png', multiplier: 1 });
      // Convert data URL to Blob using fetch — handles base64 conversion correctly
      const response = await fetch(dataUrl);
      const blob = await response.blob();
      const filename = `slide-${String(i + 1).padStart(2, '0')}.png`;
      folder.file(filename, blob);
      onProgress(Math.round(((i + 1) / canvases.length) * 100));
    }

    const zipBlob = await zip.generateAsync({ type: 'blob' });
    const url = URL.createObjectURL(zipBlob);
    const link = document.createElement('a');
    link.href = url;
    link.download = `${topicSlug}.zip`;
    document.body.appendChild(link);
    link.click();
    document.body.removeChild(link);
    URL.revokeObjectURL(url);
  }

  // Helper to convert topic title to URL-safe slug
  export function titleToSlug(title: string): string {
    return title
      .toLowerCase()
      .replace(/[^a-z0-9\s-]/g, '')
      .replace(/\s+/g, '-')
      .replace(/-+/g, '-')
      .trim();
  }

FILE 4: carousel-ui/src/export/__tests__/exportPng.test.ts

Mock the canvas object. Test:
1. exportSlideAsPng calls canvas.toDataURL with { format: 'png', multiplier: 1 }
2. exportSlideAsPng creates an anchor element and calls link.click()
3. exportSlideAsPng uses the provided filename as link.download
4. multiplier is never 2 (verify mock call args don't contain multiplier: 2)

  const mockCanvas = {
    toDataURL: vi.fn().mockReturnValue('data:image/png;base64,test'),
  };

FILE 5: carousel-ui/src/export/__tests__/exportZip.test.ts

Mock JSZip and canvas objects. Test:
1. exportAllSlidesAsZip calls onProgress with 50 after 1 of 2 slides and 100 after both
2. exportAllSlidesAsZip calls toDataURL with multiplier: 1 on each canvas
3. titleToSlug('CRISPR Gene Editing: Turning Bacteria') returns 'crispr-gene-editing-turning-bacteria'
4. titleToSlug removes special characters and colons
  </action>
  <verify>
    <automated>cd /home/deveshd/Documents/ScienceToolkit/carousel-ui && npm run test -- --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "multiplier: 1" carousel-ui/src/export/exportPng.ts returns a match
    - grep "multiplier: 1" carousel-ui/src/export/exportZip.ts returns a match
    - No line in exportPng.ts or exportZip.ts contains "devicePixelRatio"
    - grep "padStart(2, '0')" carousel-ui/src/export/exportZip.ts returns a match (zero-padded filenames)
    - grep "URL.revokeObjectURL" carousel-ui/src/export/exportZip.ts returns a match (memory cleanup)
    - npm run test -- --run exits 0 with 0 failures
  </acceptance_criteria>
  <done>
    - PNG export functions call toDataURL with multiplier:1 (confirmed by unit test)
    - ZIP export produces correctly named files (slide-01.png through slide-0N.png)
    - Progress callback called with 0-100 range
    - Test suite green
  </done>
</task>

<task type="auto">
  <name>Task 2: Build ExportPanel component, register thumbnails in canvasRegistry, wire final App.tsx</name>
  <read_first>
    - carousel-ui/src/export/exportPng.ts (exportSlideAsPng function just created)
    - carousel-ui/src/export/exportZip.ts (exportAllSlidesAsZip, titleToSlug just created)
    - carousel-ui/src/canvas/canvasRegistry.ts (getAllCanvases, registerCanvas just created)
    - carousel-ui/src/components/ThumbnailStrip.tsx (needs registerCanvas and unregisterCanvas calls added to thumbnail useEffect)
    - carousel-ui/src/store/useCarouselStore.ts (exportProgress, exportError, setExportProgress, setExportError, meta)
    - .planning/phases/05-renderer-and-export/05-CONTEXT.md (export decisions: per-slide fallback download buttons, progress indicator, ZIP export)
  </read_first>
  <files>
    carousel-ui/src/components/ExportPanel.tsx,
    carousel-ui/src/components/ThumbnailStrip.tsx,
    carousel-ui/src/App.tsx
  </files>
  <action>
FILE 1: carousel-ui/src/components/ExportPanel.tsx

Export controls panel. Contains:
- "Export All (ZIP)" button — triggers exportAllSlidesAsZip with all registered canvases
- Progress bar — visible during ZIP export (exportProgress > 0 and < 100)
- "Exported!" completion state shown briefly when exportProgress reaches 100
- Export error message display when exportError is non-null

Component behavior:
1. "Export All ZIP" button:
   - On click: get slides count from store, get meta.title for slug, call getAllCanvases()
   - Verify canvases.length === slides.length (warn in console if mismatch)
   - Set exportProgress to 0 first, then call exportAllSlidesAsZip(canvases, slug, setExportProgress)
   - On completion: exportProgress is 100 — show "Done!" message
   - On error: catch exception, call setExportError(err.message), log to console
   - After 2 seconds at 100%: reset exportProgress to 0

2. Progress bar:
   - Only visible when exportProgress > 0
   - Width is exportProgress% of the container
   - Shows percentage text label ("47%")
   - Accent color for the progress fill

3. Per-slide download (one button per slide):
   - NOT in this component — these are in ThumbnailStrip (see below)
   - ExportPanel only handles the "Export All" flow

ExportPanel styling: dark card panel below the main canvas area in App.tsx layout.

  import { useCarouselStore } from '../store/useCarouselStore';
  import { getAllCanvases } from '../canvas/canvasRegistry';
  import { exportAllSlidesAsZip, titleToSlug } from '../export/exportZip';

FILE 2: Update carousel-ui/src/components/ThumbnailStrip.tsx

Add two things to the existing ThumbnailStrip component:

A. Register/unregister each thumbnail canvas in canvasRegistry:
   In each Thumbnail's useEffect (where Fabric Canvas is initialized):
     - On mount: registerCanvas(slide.index - 1, fabricCanvas)
       (slide.index is 1-based, registry is 0-based)
     - In cleanup return: unregisterCanvas(slide.index - 1); fc.dispose()

B. Add per-slide fallback download button below each thumbnail:
   - Small button under each thumbnail miniature: "↓" icon or "PNG" text
   - On click: calls exportSlideAsPng(canvasRegistry.get(slide.index - 1)!, `slide-${String(slide.index).padStart(2, '0')}.png`)
   - This is the per-slide fallback download (XPRT-01, XPRT-03)

FILE 3: Update carousel-ui/src/App.tsx

Add ExportPanel below the main canvas area. Final App.tsx layout:

  <div className="min-h-screen bg-neutral-950 text-white flex flex-col">
    {meta && <MetaBar />}
    <DropZone>
      {slides.length > 0 ? (
        <div className="flex flex-1 overflow-hidden">
          {/* Left sidebar: thumbnails with per-slide export buttons */}
          <aside className="w-44 flex-shrink-0 border-r border-neutral-800 overflow-y-auto bg-neutral-900">
            <ThumbnailStrip />
          </aside>
          {/* Main area: canvas + export controls */}
          <main className="flex-1 flex flex-col items-center p-6 overflow-auto gap-4">
            <SlideCanvas />
            <ExportPanel />
          </main>
        </div>
      ) : (
        <div className="flex-1 flex items-center justify-center text-neutral-600 text-sm">
          Load a markdown file to begin
        </div>
      )}
    </DropZone>
  </div>
  </action>
  <verify>
    <automated>cd /home/deveshd/Documents/ScienceToolkit/carousel-ui && npm run test -- --run 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep "exportAllSlidesAsZip" carousel-ui/src/components/ExportPanel.tsx returns a match
    - grep "exportProgress" carousel-ui/src/components/ExportPanel.tsx returns a match (progress bar)
    - grep "registerCanvas" carousel-ui/src/components/ThumbnailStrip.tsx returns a match
    - grep "unregisterCanvas" carousel-ui/src/components/ThumbnailStrip.tsx returns a match (cleanup on unmount)
    - grep "exportSlideAsPng" carousel-ui/src/components/ThumbnailStrip.tsx returns a match (per-slide fallback)
    - grep "ExportPanel" carousel-ui/src/App.tsx returns a match
    - npm run test -- --run exits 0 with 0 failures
  </acceptance_criteria>
  <done>
    - After loading a markdown file, the ExportPanel is visible below the main canvas
    - Clicking "Export All (ZIP)" shows the progress bar advancing and downloads a .zip file
    - Each thumbnail has a small download button that downloads a single PNG
    - The ZIP file contains N correctly-named PNG files (slide-01.png through slide-0N.png)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual and functional verification of the complete carousel-ui app</name>
  <what-built>
    Complete Phase 5 carousel-ui application:
    - File loading via drag-drop with metadata display (LOAD-01, LOAD-02, LOAD-03)
    - Three role-aware Fabric.js slide layouts with safe zone overlay (RNDR-01, RNDR-02, RNDR-03, RNDR-04, RNDR-05)
    - Left sidebar thumbnail strip with navigation (RNDR-06)
    - Single-slide PNG export and ZIP bundle export with progress (XPRT-01, XPRT-02, XPRT-03)
  </what-built>
  <how-to-verify>
    1. Start the app: cd /home/deveshd/Documents/ScienceToolkit/carousel-ui && npm run dev
       Open http://localhost:5173 in browser

    2. DROP TEST: Drag output/2026-03-16-crispr-gene-editing.md into the browser window
       Expected: MetaBar shows "CRISPR Gene Editing: Turning Bacteria's Own Weapons Against Them · 2026-03-16 · 6 slides · Genetics / Synthetic Biology"

    3. RENDER TEST: Check the main canvas and thumbnail strip
       Expected: 6 thumbnails visible in left sidebar, Slide 1 (hook) in main canvas
       Expected: Slide 1 shows large white title text on dark background with Inter font (not Arial/sans-serif)
       Expected: Slide 2 shows smaller title + body text + slide number badge

    4. NAVIGATION TEST: Click thumbnails 2, 3, 6 in sequence
       Expected: Main canvas updates to show the correct slide each time

    5. SAFE ZONE TEST: Click the "Show Safe Zone" button
       Expected: Dashed rectangular overlay appears marking the 120/150/80px boundaries

    6. EXPORT PNG TEST: Click the download button on any thumbnail
       Expected: A .png file downloads
       Expected: Open the PNG — it must be exactly 1080x1080 pixels (check in image viewer or: file details > dimensions)
       Expected: Font renders as Inter (rounded numerals, clean sans-serif) NOT system Arial

    7. EXPORT ZIP TEST: Click "Export All (ZIP)" button
       Expected: Progress bar advances from 0% to 100%
       Expected: A .zip file downloads named after the topic slug (e.g. "crispr-gene-editing-turning-bacterias-own-weapons-against-them.zip" or similar)
       Expected: Unzip and verify: exactly 6 files named slide-01.png through slide-06.png
       Expected: Each PNG is 1080x1080 and shows the correct slide content

    8. INVALID FILE TEST: Drag a .txt or .csv file onto the app
       Expected: Red error message appears, no page reload, app still functional

    9. FILE RELOAD TEST: With crispr file loaded, drag output/2026-03-16-solar-fuel-conversion.md
       Expected: Session updates immediately to solar fuel content, no confirmation dialog
  </how-to-verify>
  <action>Human visual verification — see how-to-verify steps above</action>
  <verify><automated>MANUAL — see how-to-verify steps</automated></verify>
  <done>All 9 verification checks pass and user types "approved"</done>
  <resume-signal>
    If all 9 checks pass: type "approved"
    If any check fails: describe which check failed and what was observed instead
  </resume-signal>
</task>

</tasks>

<verification>
Full test suite must be green before the visual checkpoint:

  cd /home/deveshd/Documents/ScienceToolkit/carousel-ui && npm run test -- --run --reporter=verbose

All test files must pass:
- src/parser/__tests__/parseMarkdown.test.ts
- src/store/__tests__/useCarouselStore.test.ts
- src/components/__tests__/DropZone.test.tsx
- src/canvas/__tests__/constants.test.ts
- src/canvas/__tests__/layouts.test.ts
- src/components/__tests__/ThumbnailStrip.test.tsx
- src/export/__tests__/exportPng.test.ts
- src/export/__tests__/exportZip.test.ts

Critical assertions (run before checkpoint):
  grep "multiplier: 1" /home/deveshd/Documents/ScienceToolkit/carousel-ui/src/export/exportPng.ts
  Must return a match — export fidelity depends on this.

  grep "pixelRatio: 1" /home/deveshd/Documents/ScienceToolkit/carousel-ui/src/components/SlideCanvas.tsx
  Must return a match — canvas size correctness depends on this.

  grep "pixelRatio: 1" /home/deveshd/Documents/ScienceToolkit/carousel-ui/src/components/ThumbnailStrip.tsx
  Must return a match — thumbnails must also be at correct resolution for export.
</verification>

<success_criteria>
1. npm run test -- --run exits 0 with 0 failures (all 8 test files)
2. Exported single PNGs are exactly 1080x1080 pixels when measured in an image viewer
3. ZIP bundle contains N slides correctly named slide-01.png through slide-0N.png
4. Exported PNGs use Inter font (not system Arial/Helvetica fallbacks)
5. Checkpoint human-verify task returns "approved" for all 9 verification checks
6. Phase 5 requirements complete: LOAD-01, LOAD-02, LOAD-03, RNDR-01 through RNDR-06, XPRT-01, XPRT-02, XPRT-03
</success_criteria>

<output>
After completion, create .planning/phases/05-renderer-and-export/05-03-SUMMARY.md following the summary template at @/home/deveshd/.claude/get-shit-done/templates/summary.md
</output>
